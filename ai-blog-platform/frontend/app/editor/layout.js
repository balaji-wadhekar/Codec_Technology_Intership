export const metadata = {
  title: 'AI Editor | AI Blog Platform',
  description: 'Write your next blog post with AI assistance',
};

export default function EditorLayout({ children }) {
  return <>{children}</>;
}
